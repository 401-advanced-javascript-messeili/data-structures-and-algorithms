{
  "name": "arrayShift",
  "version": "1.0.0",
  "description": "write a function which takes in an array and the value to be added and return an array with the new value added to the middle index.",
  "main": "array-shift.js",
  "scripts": {
    "test": "jest --verbose --coverage",
    "test-watch": "jest --verbose --coverage --watchAll"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "D": "^1.0.0",
    "jest": "^26.5.3"
  }
}
